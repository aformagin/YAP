# Copy this file to .env and fill in the values before running Docker.
# The .env file is read automatically by `docker compose`.

# -----------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------

# CHANGE THIS — a long, random string used to sign JWT tokens.
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=change-this-to-a-long-random-secret

# Absolute path on the HOST machine to your music library.
# The directory is mounted read-only inside the container at /music.
# When scanning from the Admin panel, enter "/music" as the directory path.
MUSIC_DIR=/path/to/your/music

# -----------------------------------------------------------------------
# Optional overrides
# -----------------------------------------------------------------------

# Host port to expose the app on (default: 80)
# APP_PORT=80

# Override the CORS origin reported by the backend (rarely needed behind nginx)
# CORS_ORIGIN=http://localhost

# Override the API base URL baked into the frontend at build time.
# Leave empty (default) to use nginx reverse-proxy (recommended).
# Set to an explicit URL only if you need clients to reach the API directly,
# e.g. a mobile device on the same LAN accessing a non-proxied endpoint.
# VITE_API_BASE_URL=http://192.168.1.100:3000
