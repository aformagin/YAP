# Dependencies — reinstalled inside the image
**/node_modules

# Build output — rebuilt inside the image
**/dist

# Runtime data — provided by volume mounts
**/yap_media.db
**/.cache

# Git
.git
.gitignore

# Env files — injected at runtime via docker compose
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
*.log

# Instruction files used only during development
**/developer_agent_*.md
